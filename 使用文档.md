# wireguard-go 使用文档

本仓库是 WireGuard 的 Go 语言用户态实现（fork 版本）。相比上游，它支持通过 `.conf` 配置文件一键启动，并内置了配置同步、自更新等扩展能力。

## 1. 环境要求

- Go 1.23.1 或更高
- Linux（创建 TUN 虚拟网卡需要 root 或 `CAP_NET_ADMIN`）

> 说明：Linux 内核通常已原生支持 WireGuard，内核模块更快、集成更好；本实现主要用于内核模块不可用、或需要用户态运行的场景。

## 2. 构建与安装

### 2.1 一键安装（推荐）

```bash
sudo ./install-wireguard-go.sh                 # 编译 + 安装 + systemd 开机自启
sudo ./install-wireguard-go.sh --skip-systemd  # 只装二进制与配置，不装 systemd
```

脚本会完成：

1. 编译 `wireguard-go` 到 `/opt/wireguard/`
2. 生成/复用客户端密钥对与 `/opt/wireguard/<接口>.conf`（已存在则复用，身份不变）
3. 生成 systemd 单元 + 路由脚本，并 `enable --now`

脚本顶部集中了可改参数（服务器公钥/端点、客户端地址、AllowedIPs、接口名、MTU、保活、版本号等），换机器或换参数时改那一节即可。

### 2.2 手动构建

```bash
make                                             # 自动注入版本号
# 或
go build -trimpath -o wireguard-go .             # 版本号显示 0.0.1
```

## 3. 配置文件格式

`.conf` 采用标准 wg-quick 的 `[Interface]` / `[Peer]` 格式。

**接口名 = 配置文件名**：例如 `wg0.conf` 会创建名为 `wg0` 的接口；传入目录时，目录内每个 `.conf` 对应一个接口。

```ini
[Interface]
PrivateKey = <本机私钥，base64 编码的 32 字节>
Address = 192.168.190.22/32
MTU = 1420

[Peer]
PublicKey = <服务器公钥>
Endpoint = 101.133.133.127:8357
AllowedIPs = 192.168.190.0/24
PersistentKeepalive = 25
```

### 支持字段

| 段 | 字段 | 说明 |
|----|------|------|
| `[Interface]` | `PrivateKey` | 私钥（base64，32 字节） |
| | `ListenPort` | 监听端口 |
| | `FwMark` | 路由标记 |
| | `MTU` | 接口 MTU |
| | `Address` | 地址，**需带前缀**（如 `192.168.190.22/32`） |
| `[Peer]` | `PublicKey` | 对端公钥 |
| | `PresharedKey` | 预共享密钥 |
| | `AllowedIPs` | 允许 IP（逗号分隔） |
| | `Endpoint` | 对端 `IP:端口` |
| | `PersistentKeepalive` | 保活间隔（秒） |
| | `Name` | 自定义名称（本 fork 扩展） |

被忽略（不生效）的字段：`DNS`、`Table`、`PreUp`、`PostUp`、`PreDown`、`PostDown`、`SaveConfig` —— 这些脚本钩子不会被解析执行。

## 4. 密钥生成

未安装 `wireguard-tools`（没有 `wg genkey`）时，可用 Go 标准库 `crypto/ecdh` 生成 X25519 密钥对：

```go
package main

import (
	"crypto/ecdh"
	"crypto/rand"
	"encoding/base64"
	"fmt"
)

func main() {
	priv, _ := ecdh.X25519().GenerateKey(rand.Reader)
	fmt.Println("私钥:", base64.StdEncoding.EncodeToString(priv.Bytes()))
	fmt.Println("公钥:", base64.StdEncoding.EncodeToString(priv.PublicKey().Bytes()))
}
```

## 5. 运行

### 5.1 systemd（推荐）

安装脚本已生成 `wireguard-go.service`：

```bash
sudo systemctl start   wireguard-go.service   # 启动
sudo systemctl stop    wireguard-go.service   # 停止
sudo systemctl restart wireguard-go.service   # 重启
sudo systemctl status  wireguard-go.service   # 查看状态
sudo systemctl enable  wireguard-go.service   # 开机自启
sudo systemctl disable wireguard-go.service   # 取消自启
```

停止时 `device.Close()` 会删除 TUN 接口，相关路由由内核自动清理。

### 5.2 前台运行

```bash
sudo wireguard-go -f -c /opt/wireguard/wg0.conf
```

### 5.3 后台守护（默认）

```bash
sudo wireguard-go -c /opt/wireguard/wg0.conf   # 默认后台运行，PID 文件 /var/run/wireguard-go.pid
sudo wireguard-go -q                          # 停止后台守护进程
sudo wireguard-go -S                          # 查看守护进程状态
```

## 6. 路由（重要）

本 fork 启动时只会**分配地址、设置 MTU、`ip link up`**，**不会自动添加路由**（`PostUp` 等钩子被忽略）。分流隧道需要手动添加路由：

```bash
sudo ip route add 192.168.190.0/24 dev wg0
```

安装脚本已通过 systemd 的 `ExecStartPost`（`<接口>-route.sh`）自动重试添加路由，无需手动干预。

## 7. 服务器端登记 peer

在 WireGuard 服务器上，把客户端公钥登记为 peer：

```bash
# 服务器上执行（<接口> 为服务器侧接口名，如 wgtun5）
wg set <接口> peer <客户端公钥> allowed-ips <客户端IP>/32 persistent-keepalive 25
```

同时要把该 `[Peer]` 追加到服务器配置文件（如 `/etc/wireguard/<接口>.conf`），否则重启后丢失。

## 8. 命令行参数与环境变量

### 参数

| 参数 | 说明 |
|------|------|
| `-c, --confile <路径>` | 配置来源：`.conf` / `.zip` / 目录 |
| `-f, --foreground` | 前台运行 |
| `-q, --quit` | 停止后台守护进程 |
| `-S, --status` | 查看守护进程状态 |
| `--version` | 显示版本 |
| `--sync ...` 系列 | 配置同步（上传/下载到 GitHub/Gitee Gist，显式触发） |

### 环境变量

| 变量 | 说明 |
|------|------|
| `confile` | 等效于 `--confile`，命令行优先 |
| `log_level` | 日志级别 0-7 |
| `log_name` | 日志文件名标识 |
| `log_server` | 远程日志服务器 `host:port` |

## 9. 日志

- 文件日志：`/tmp/ulog_WireGuard.txt`（可用 `log_name` 改标识）
- systemd 日志：`sudo journalctl -u wireguard-go.service -f`

## 10. 注意事项

1. **自更新**：`wireguard-go --upgrade` 是手动触发的二进制自更新（走明文 HTTP 版本服务器）；**正常启动不联网**，不会自动检查更新。
2. **版本号**：普通 `go build` 编译时 `--version` 显示 `0.0.1`；用 `make` 或 `-ldflags "-X main.appVer=vX.Y.Z"` 注入正式版本号，`--upgrade` 的版本比对才正常。
3. **sudo 下编译**：`sudo` 会重置环境，安装脚本已内置 `GOPROXY=goproxy.cn` 与模块缓存复用；手动编译时需自行 `export GOPROXY=https://goproxy.cn,direct`。

## 11. 常见问题

| 现象 | 原因与处理 |
|------|-----------|
| 服务启动失败：`Device for nexthop is not up` | 路由加早了，接口尚未 `up`；用重试方式加路由（脚本已处理） |
| 编译报 `dial tcp ... i/o timeout` | 模块代理被墙；设置 `GOPROXY=https://goproxy.cn,direct` |
| `--version` 显示 `0.0.1` | 未注入版本号；见第 10 节第 2 条 |
| 能 ping 通接口地址但无法访问对端 | 缺少分流路由；见第 6 节 |
