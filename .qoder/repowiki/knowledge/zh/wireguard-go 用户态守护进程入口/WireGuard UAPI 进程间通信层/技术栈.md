Go 构建标签（`linux`/`darwin`/`freebsd`/`openbsd`/`windows`/`wasm`）进行平台分发；Unix 侧使用 `golang.org/x/sys/unix` 的 inotify/kqueue 与 `net.UnixListener`；Windows 侧使用 `golang.org/x/sys/windows` 的 Named Pipe 原生 API（`NtCreateNamedPipeFile`、`ConnectNamedPipe`），并通过 `wireguard/rwcancel` 实现可取消的 I/O。