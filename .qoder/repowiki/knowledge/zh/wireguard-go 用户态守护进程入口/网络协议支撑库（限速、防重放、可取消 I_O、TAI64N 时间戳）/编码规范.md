- 每个子包均包含同名 `_test.go` 单元测试文件，测试与实现一一对应放置。
- 所有源文件顶部保留 SPDX 许可证头与 WireGuard LLC 版权注释。
- 平台相关实现通过 `//go:build` 构建标签拆分为主实现与 stub 文件（如 `rwcancel.go` 与 `rwcancel_stub.go`）。
- 对外暴露的最小接口结构体配合方法接收者封装状态（如 `Filter`、`RWCancel`、`Ratelimiter`），避免全局可变状态。