- 平台差异通过 build tag 拆分到 main.go（!windows）与 main_windows.go，共享逻辑集中在同一 package main 中
- 外部子系统通过环境变量（WG_TUN_FD、WG_UAPI_FD、LOG_LEVEL 等）进行配置注入，而非命令行参数或配置文件
- 错误路径统一输出到 device.NewLogger 并返回 ExitSetupFailed(1)，成功时返回 ExitSetupSuccess(0)