Unix 侧默认 socket 目录为 `/var/run/wireguard`，该路径由 `socketDirectory` 变量承载，可通过 linker flag 在 wireguard-android 构建时替换；Windows 侧命名管道路径固定为 `\\.\pipe\ProtectedPrefix\Administrators\WireGuard\<iface>`，且需在 `init()` 中设置管理员权限的安全描述符。