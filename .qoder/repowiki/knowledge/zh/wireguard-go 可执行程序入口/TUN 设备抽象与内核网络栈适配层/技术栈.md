Linux 端使用 `golang.org/x/sys/unix` 直接调用 ioctl/netlink，并启用 virtio_net_hdr 的 TCP/UDP GRO/GSO 卸载；Windows 端使用 `golang.zx2c4.com/wintun` 用户态驱动；纯 Go 虚拟设备基于 `gvisor.dev/gvisor/pkg/tcpip` 协议栈，DNS 解析使用 `golang.org/x/net/dns/dnsmessage` 手工编解码。