- 通过 //go:build 标签在同一包内并列放置同名符号的多份实现，由编译器按目标平台选择单一版本注入。
- 队列常量在各平台文件中保持完全一致的符号名（QueueStagedSize、QueueOutboundSize、QueueInboundSize、QueueHandshakeSize、MaxSegmentSize、PreallocatedBuffersPerPool），仅数值不同，便于上层无感知复用。
- 对平台无关或无需实现的接口（如 startRouteListener）提供空实现（返回 nil, nil），使调用方无需条件分支即可跨平台调用。