- 对象池通过 WaitPool 封装，Get/Put 成对出现，Put 前必须清空内部指针或重置切片以避免内存泄漏。
- 日志输出统一通过 Logger 的 Verbosef/Errorf 函数指针进行，默认置为空函数 DiscardLogf 以实现零开销静默。
- TUN 事件处理在独立协程 RoutineTUNEventReader 中通过 range device.tun.device.Events() 循环驱动，并对 MTU 做 MaxContentSize 上界裁剪。